name: clean_feature
description: This is a brick that created a feature, at specified place, that follows clean architecture

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.26 <0.1.0"

vars:
  featureName:
    type: string
    description: Clean Feature name
    default: New Feature
    prompt: What's the feature name?
